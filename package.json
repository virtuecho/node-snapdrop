{
  "name": "snapdrop",
  "private": true,
  "version": "1.0.0",
  "description": "A pre-LimeWire Snapdrop with a complete Node.js server.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "dev:public": "node index.js public",
    "lint": "node scripts/check-syntax.js",
    "test": "node scripts/smoke-test.js",
    "smoke": "node scripts/smoke-test.js",
    "arch:check": "node scripts/check-architecture.js",
    "build": "node scripts/check-syntax.js",
    "check": "pnpm run lint && pnpm run arch:check && pnpm run test && pnpm run build",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "engines": {
    "node": ">= 18"
  },
  "author": "",
  "license": "GPL-3.0-only",
  "packageManager": "pnpm@10.33.2",
  "dependencies": {
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.1",
    "ua-parser-js": "^2.0.9",
    "unique-names-generator": "^4.3.0",
    "ws": "^8.20.0"
  },
  "devDependencies": {
    "wrangler": "^4.90.1"
  }
}
